lookup_options:
  mcollective_agent_puppetca::gem_dependencies:
    merge:
      strategy: deep
  mcollective_agent_puppetca::package_dependencies:
    merge:
      strategy: deep
  mcollective_agent_puppetca::config:
    merge:
      strategy: deep
  mcollective_agent_puppetca::client_config:
    merge:
      strategy: deep
  mcollective_agent_puppetca::server_config:
    merge:
      strategy: deep
